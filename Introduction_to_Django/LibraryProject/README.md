# LibraryProject

This is a basic Django project created as part of the **Introduction to Django Development Environment Setup** task in the `Alx_DjangoLearnLab` repository.

## 🚀 Project Setup

### 1. Install Django
Make sure Python and pip are installed, then run:
```bash
pip install django